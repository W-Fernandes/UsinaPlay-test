export interface UserData {
  name_user: string;
  avatar: string;
  nivel: string;
}

export interface Slide {
  banner: string;
  label: string;
}

export interface Notification {
  titulo: string;
  mensagem: string;
  lido: boolean;
}
